{
  "name": "crypto-drive",
  "version": "1.0.0",
  "private": true,
  "description": "CryptoDrive — Web3 file storage on Shelby + Aptos",
  "scripts": {
    "dev:api": "npm run start:dev --prefix apps/api",
    "dev:web": "npm run dev:isolated --prefix apps/web",
    "build:api": "npm run build --prefix apps/api",
    "build:web": "npm run build --prefix apps/web",
    "generate": "npm run generate --prefix apps/api",
    "migrate": "npm run migrate --prefix apps/api",
    "studio": "npm run studio --prefix apps/api"
  }
}
